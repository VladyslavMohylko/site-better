const swiper = new Swiper('.slider-main-block', {
    // Optional parameters
    loop: true,
  
    // Navigation arrows
    navigation: {
      nextEl: '.body-main-block_arrow.swiper-button-next',
      prevEl: '.body-main-block_arrow.swiper-button-prev',
    },
});